{
  "name": "universal-hackathon-boilerplate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "install-all": "npm install && npm install --prefix client && npm install --prefix server",
  "reinstall": "rm -rf node_modules client/node_modules server/node_modules package-lock.json client/package-lock.json server/package-lock.json && npm install && npm install --prefix client && npm install --prefix server",
    "client": "npm start --prefix client",
    "server": "nodemon server/src/server.js",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}